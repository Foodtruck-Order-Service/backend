<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.fos.Option2Mapper">
    <resultMap id="option2ResultMap" type="kr.co.fos.Option2">
        <result column="NO" property="no" />
        <result column="MENU_NO" property="menuNo" />
        <result column="OPTION_NAME" property="optionName" />
    </resultMap>

    <sql id="whereSql">
        <where>
            <if test="no != null and no != ''">
                AND NO = #{no}
            </if>
            <if test="menuNo != null and menuNo != ''">
                AND MENU_NO = #{menuNo}
            </if>
            <if test="optionName != null and optionName != ''">
                AND OPTION_NAME = #{optionName}
            </if>
        </where>
    </sql>

    <select id="count" parameterType="kr.co.fos.Option2" resultType="int">
        SELECT COUNT(*)
        FROM option2
        <include refid="whereSql" />
    </select>

    <select id="list" parameterType="kr.co.fos.Option2" resultMap="option2ResultMap">
        SELECT *
        FROM (
            SELECT
                NO,
                MENU_NO,
                OPTION_NAME
            FROM option2
            <include refid="whereSql" />
        ) TB
        <where>
            <if test="itemStart > -1">
                RN BETWEEN (#{itemStart} + 1)
                       AND (#{itemStart} + #{itemSizePerPage})
            </if>
        </where>
    </select>

    <select id="select" parameterType="kr.co.fos.Option2" resultMap="option2ResultMap">
        SELECT
            NO,
            MENU_NO,
            OPTION_NAME
        FROM option2
        <include refid="whereSql" />
    </select>

    <insert id="insert" parameterType="kr.co.fos.Option2">
        INSERT INTO option2 (
            NO,
            MENU_NO,
            OPTION_NAME
        ) VALUES (
            #{no},
            #{menuNo},
            #{optionName}
        )
    </insert>

    <update id="update" parameterType="kr.co.fos.Option2">
        UPDATE option2
        <set>
            <if test="no != ''">
                NO = #{no},
            </if>
            <if test="menuNo != ''">
                MENU_NO = #{menuNo},
            </if>
            <if test="optionName != ''">
                OPTION_NAME = #{optionName},
            </if>
        </set>
    </update>

    <delete id="delete" parameterType="kr.co.fos.Option2">
        DELETE FROM option2
        <include refid="whereSql" />
    </delete>
</mapper>